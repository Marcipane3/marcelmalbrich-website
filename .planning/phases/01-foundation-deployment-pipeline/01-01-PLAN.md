---
phase: 01-foundation-deployment-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/package.json
  - main/next.config.js (or .ts, per create-next-app output)
  - main/tsconfig.json
  - main/tailwind.config.ts
  - main/postcss.config.mjs
  - main/app/layout.tsx
  - main/app/page.tsx
  - main/app/globals.css
  - main/.gitignore
  - .gitignore (repo root, if needed)
  - README.md (repo root)
autonomous: false
requirements: [INFRA-02]
must_haves:
  truths:
    - "Marcel can open the GitHub repo in a browser and see the full Next.js scaffold plus a README describing the project"
    - "A visitor running the dev server locally sees Marcel's name and a 'site under construction' line, not a default Next.js starter page"
  artifacts:
    - path: "main/app/page.tsx"
      provides: "Placeholder home page with semantic h1 (name) and p (tagline)"
      min_lines: 8
    - path: "main/app/layout.tsx"
      provides: "Root layout with <title> metadata set to Marcel Malbrich"
      min_lines: 10
    - path: "README.md"
      provides: "Project description for GitHub repo presentation"
      min_lines: 5
  key_links:
    - from: "main/app/page.tsx"
      to: "main/app/layout.tsx"
      via: "Next.js App Router page-under-layout convention"
      pattern: "export default function"
---

<objective>
Scaffold the Next.js + Tailwind application inside a `/main` folder of this repo, build the minimal placeholder page Marcel approved in CONTEXT.md (D-07) and UI-SPEC.md, and get the result version-controlled in a new public GitHub repository under Marcel's account.

Purpose: This is the first concrete, inspectable artifact of the entire project — code that exists, is structured the way later phases will build on, and is visible to Marcel in GitHub. It is also the prerequisite for Plan 02's Vercel deployment (Vercel deploys from a GitHub repo, so the repo must exist and contain a working app first).
Output: A `/main` Next.js App Router + Tailwind CSS application with one placeholder page, pushed to a new public GitHub repository `Marcipane3/<repo-name>` with a README.md at the repo root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-deployment-pipeline/01-UI-SPEC.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 portion)

- `main/` — folder holding the entire main-site Next.js application (sibling will be `lab/` in Phase 4; do not create `lab/` now — out of scope per CONTEXT.md D-02 and PATTERNS.md)
- `main/package.json` — npm-managed Next.js + Tailwind + TypeScript project manifest
- `main/app/layout.tsx` — root layout exporting `metadata` object with `title: "Marcel Malbrich"`
- `main/app/page.tsx` — default export functional component rendering `<h1>` (name) and `<p>` (tagline)
- `main/app/globals.css` — Tailwind directives (`@tailwind base/components/utilities` or v4 `@import "tailwindcss"` per scaffold version), unmodified default theme (no custom tokens — Phase 2 owns tokens)
- `main/tailwind.config.ts` — default/unmodified Tailwind config from scaffold (no custom colors/fonts added this phase)
- `README.md` (repo root) — project description per CONTEXT.md D-05
- New GitHub repository: `Marcipane3/<repo-name>` (public, per CONTEXT.md D-04), containing this commit history
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js + Tailwind app in /main and build the placeholder page</name>
  <files>main/package.json, main/app/layout.tsx, main/app/page.tsx, main/app/globals.css, main/tailwind.config.ts, main/tsconfig.json, main/.gitignore</files>
  <read_first>
    .planning/phases/01-foundation-deployment-pipeline/01-CONTEXT.md (D-07 — placeholder content rules)
    .planning/phases/01-foundation-deployment-pipeline/01-UI-SPEC.md (exact typography/color/structural requirements for this page)
    CLAUDE.md (Technical Conventions section — Next.js App Router + Tailwind, design tokens centralized, no ad hoc styles)
  </read_first>
  <action>
    Run `npx create-next-app@latest main` from the repo root, answering the scaffold prompts as: TypeScript = Yes, ESLint = Yes, Tailwind CSS = Yes, `src/` directory = No, App Router = Yes, customize import alias = No (keep default `@/*`). This creates the `main/` folder with `app/`, `package.json`, `tailwind.config.ts` (or `.js`/v4-style CSS-only config depending on the scaffolded Tailwind version — use whatever the tool generates, do not hand-roll), and a `.gitignore` scoped to `main/`.

    Do not run `npm run dev` interactively and leave it running — verify with a one-shot build/lint command instead (see verify).

    Edit `main/app/layout.tsx`: keep the generated `RootLayout` component structure, but replace the generated `metadata` export's `title` and `description` fields with `title: "Marcel Malbrich"` and a short description (e.g. "Personal portfolio site — under construction"). Do not add any custom fonts, color classes, or dark-mode `<html className>` logic — leave the `<html>`/`<body>` tags exactly as scaffolded (UI-SPEC: no design tokens this phase).

    Replace the entire generated content of `main/app/page.tsx` (delete the default Next.js starter markup — logo, links, footer grid) with a minimal component: a single `<main>` wrapping a centered `<h1>` containing Marcel's name and a `<p>` containing a short "under construction" line. Confirm exact wording with Marcel before finalizing if not already provided (UI-SPEC default suggestion: heading "Marcel Malbrich", body "Site under construction — check back soon."). Apply only plain Tailwind utility classes for centering and spacing per UI-SPEC's declared values (e.g. `flex min-h-screen flex-col items-center justify-center gap-6 p-4` — gap-6 = 24px matches UI-SPEC `lg` token, p-4 = 16px matches `md` token); do not introduce custom colors, custom font-family classes, or any dark-mode variant classes. Heading text size should resolve to roughly 28px/semibold (UI-SPEC heading role) and body text to roughly 16px/regular (UI-SPEC body role) using Tailwind's default type scale (e.g. `text-3xl font-semibold` for heading, default body text for the paragraph) — do not hardcode font-size or font-weight outside Tailwind utility classes.

    Leave `main/tailwind.config.ts`, `main/tsconfig.json`, and `main/package.json` exactly as generated by the scaffold — no edits, no added dependencies, no custom theme extension (Phase 2 owns tokens).
  </action>
  <acceptance_criteria>
    - `main/package.json` exists and contains `"next"` as a dependency
    - `main/app/page.tsx` contains exactly one `<h1` tag and at least one `<p` tag, with no occurrence of the default Next.js starter strings ("Get started by editing", "Deploy now", "vercel.svg")
    - `main/app/layout.tsx` contains `title: "Marcel Malbrich"` inside its `metadata` export
    - `main/tailwind.config.ts` (or equivalent generated config) contains no manual edits beyond scaffold defaults (diff against a fresh `create-next-app` output would show no theme/color changes)
    - No `className` in `main/app/page.tsx` or `main/app/layout.tsx` references dark mode (no `dark:` Tailwind variants present)
  </acceptance_criteria>
  <verify>
    <automated>cd main && npm run build</automated>
  </verify>
  <done>`npm run build` completes successfully inside `main/` with zero errors, producing a `.next` build output, and `main/app/page.tsx` renders only the name heading and tagline paragraph (verified by reading the file content, no starter boilerplate remaining).</done>
</task>

<task type="auto">
  <name>Task 2: Create README and push the repo to a new public GitHub repository</name>
  <files>README.md, .gitignore (repo root, if main/.gitignore doesn't already cover build artifacts at root level)</files>
  <read_first>
    .planning/phases/01-foundation-deployment-pipeline/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-05 — repo structure, account, visibility, README requirement)
    .planning/PROJECT.md (project description content to draw from for README)
  </read_first>
  <action>
    Write `README.md` at the repo root (not inside `main/`) describing the project: name it "Marcel Malbrich — Personal Website", summarize the three purposes from PROJECT.md ("What This Is" section — professional portfolio, learning vehicle, hidden lab playground), note the tech stack (Next.js App Router, Tailwind CSS, Vercel, GitHub), and note the repo layout (`main/` = production site, `lab/` = experimental playground arriving in a later phase, per D-01/D-02). Keep it concise — this is D-05's minimum bar, not the "fancy HTML overview page" Marcel mentioned, which is explicitly deferred (see CONTEXT.md Deferred Ideas).

    Confirm the repo root `.gitignore` (or `main/.gitignore` from scaffold) excludes `node_modules/`, `.next/`, and `.env*` — if `create-next-app` already wrote a `.gitignore` inside `main/` covering these, that is sufficient; do not duplicate it at root unless root-level tooling is later added.

    Create the new GitHub repository non-interactively via `gh repo create Marcipane3/marcelmalbrich-website --public --description "Marcel Malbrich personal portfolio website (Next.js + Tailwind + Vercel)" --source=. --remote=origin` run from the repo root (gh CLI is already authenticated as `Marcipane3` — confirmed, no auth gate expected). Stage and commit all current work (`main/` scaffold, `README.md`, any `.gitignore`) with a message like `feat(01): scaffold Next.js app and add placeholder page`, then push to `origin main` (or `origin master`, matching whatever branch name `git branch` currently reports — this repo's existing local branch is `master`, so push that branch name and set it as the default/upstream).
  </action>
  <acceptance_criteria>
    - `gh repo view Marcipane3/marcelmalbrich-website` succeeds and shows `public` visibility
    - `git remote -v` shows an `origin` remote pointing at `github.com:Marcipane3/marcelmalbrich-website` (or `https://github.com/Marcipane3/marcelmalbrich-website`)
    - `git log origin/master` (or `origin/main`) shows the scaffold commit pushed successfully
    - `README.md` exists at repo root and is at least 5 lines long, mentioning Next.js, Tailwind, and Vercel
    - `main/node_modules/` and `main/.next/` are NOT present in `git ls-files` output (properly gitignored)
  </acceptance_criteria>
  <verify>
    <automated>gh repo view Marcipane3/marcelmalbrich-website --json visibility,url 2>&1 && git ls-files | grep -v node_modules | grep -v "\.next/" | wc -l</automated>
  </verify>
  <done>The GitHub repository `Marcipane3/marcelmalbrich-website` exists, is public, contains the full `main/` Next.js scaffold plus root `README.md`, and Marcel can open the repo URL in a browser to see it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local machine → GitHub | `gh` CLI push sends full repo contents (code + git history) to a public remote — anything committed becomes publicly visible permanently (even if later deleted, history persists unless rewritten) |
| Public repo → anyone on the internet | Repo is public per D-04 — any secrets accidentally committed are exposed to the world, not just Marcel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-01 | Information Disclosure | `.gitignore` / repo push | mitigate | Task 2 explicitly verifies `node_modules/` and `.next/` are excluded before push; no `.env` files are created in this phase (no secrets exist yet — Vercel tokens are handled via CLI auth in Plan 02, never written to a file in this repo) |
| T-01-02 | Information Disclosure | Public repo visibility (D-04) | accept | Repo is intentionally public per Marcel's explicit decision (D-04) — content is a portfolio site with no confidential data; PPM Rollout content sanitization is enforced in Phase 3, not this phase |
| T-01-SC | Tampering | npm install via `create-next-app` | mitigate | `create-next-app` and its transitive dependencies (`next`, `react`, `tailwindcss`) are first-party/extremely high-trust packages with no `[ASSUMED]`/`[SUS]` status — no additional third-party packages are installed in this plan, so no package legitimacy checkpoint is required |

</threat_model>

<verification>
1. `cd main && npm run build` exits 0 with no TypeScript/ESLint errors.
2. `main/app/page.tsx` contains the placeholder heading + tagline only, no Next.js starter boilerplate.
3. `gh repo view Marcipane3/marcelmalbrich-website` confirms the repo exists, is public, and the local `master` branch is pushed and tracked against `origin`.
4. Marcel can open `https://github.com/Marcipane3/marcelmalbrich-website` in a browser and see the `main/` folder and `README.md`.
</verification>

<success_criteria>
- A new public GitHub repository exists under `Marcipane3` containing a working Next.js + Tailwind scaffold inside `main/` and a root `README.md`.
- The placeholder page at `main/app/page.tsx` shows only Marcel's name and a short "under construction" line, with no leftover starter content, no dark mode, no custom design tokens.
- All work is committed and pushed — nothing is local-only at the end of this plan, since Plan 02 needs the GitHub repo populated before Vercel can deploy from it.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-deployment-pipeline/01-01-SUMMARY.md` when done
</output>
